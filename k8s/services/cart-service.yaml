apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: shopsphere
  labels:
    app: cart-service
    app.kubernetes.io/name: cart-service
    app.kubernetes.io/part-of: shopsphere
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
        app.kubernetes.io/name: cart-service
        app.kubernetes.io/part-of: shopsphere
    spec:
      containers:
        - name: cart-service
          image: shopsphere/cart-service:0.1
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 4003
              protocol: TCP

          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=60"          
            
            - name: SERVER_PORT
              value: "4003"

            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://mysql:3306/shopsphere_cart?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC"

            - name: SPRING_DATASOURCE_USERNAME
              value: "root"

            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shopsphere-secrets
                  key: MYSQL_ROOT_PASSWORD

            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: shopsphere-config
                  key: KAFKA_BOOTSTRAP_SERVERS

          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "300Mi"

          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5

---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: shopsphere
  labels:
    app: cart-service
spec:
  type: ClusterIP
  selector:
    app: cart-service
  ports:
    - name: http
      port: 4003
      targetPort: 4003
      protocol: TCP